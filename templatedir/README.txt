This is the template folder for the simulations. 
The script create_inputfiles.py creates a copy of this folder for each element of the ensemble. 
All the files with parameters to replace using the values saved in samples.csv should be in this folder.

